<div class="jumbotron">
  <%= form_for @model_new_data, url: fetch_model_infos_create_path(:model_create_data => @model_new_data), method: :post do |f| %>
      <% (@model_new_data.class.column_names - %w(id created_at updated_at)).each do |resource_field| %>
          <div class="form-group">
            <%= f.label resource_field %>
            <div class="form-group">
              <%= f.text_field resource_field %>
            </div>
          </div>
      <% end %>
      <button class="btn btn-primary" type="submit">Save Changes</button>
  <% end %>
</div>